This 'extractors/' folder is used to overwrite/add a Turndown's rules to allow Turndown to convert the content more accurately than it might otherwise do.
See : https://github.com/mixmark-io/turndown
